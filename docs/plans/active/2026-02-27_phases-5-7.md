# Phases 5-7: Richer KB, Resilience, UX Polish

**Created**: 2026-02-27
**Status**: active
**Estimated**: 4-6 hours, ~2000 lines

## Objective

Deepen KB integration (Phase 5), harden for production (Phase 6), polish
report quality and UX (Phase 7). Starting from Phase 4 baseline: 298 tests,
91% coverage, 8/21 MCP tools wrapped.

## Phase 5: Richer KB Integration (5 commits)

### Step 1: MCP client + state model + config
- `mcp_client.py`: +5 methods (get_source, find_similar_concepts, cross_domain_concepts, list_domains, stats)
- `state.py`: +5 ResearchState fields, +5 NodeUpdate fields
- `config.py`: +max_similar_concepts, +enable_cross_domain
- Tests for all new methods and fields

### Step 2: Pre-pipeline KB context
- `graph.py`: list_domains + stats before graph invocation
- `query_planner.py`: SYSTEM_PROMPT includes domain list + corpus stats
- `synthesis.py`: kb_stats_summary in evidence metadata

### Step 3: Source enrichment in citation_analyzer
- `citation_analyzer.py`: get_source for top-cited sources
- `synthesis.py`: source details section

### Step 4: Embedding-based concept discovery
- `concept_explorer.py`: find_similar_concepts in _explore_one
- `synthesis.py`: similar concepts subsection

### Step 5: Cross-domain concept bridging
- `concept_explorer.py`: cross_domain_concepts post-exploration phase
- `synthesis.py`: cross-domain bridges section

## Phase 6: Resilience & Observability (4 commits)

### 6.1: Per-node timeouts
### 6.2: Timing in StreamEvent
### 6.3: Graceful node failure
### 6.4: Health check + coverage hardening

## Phase 7: Report Quality & UX (4 commits)

### 7.1: Structured JSON output (--json flag)
### 7.2: Per-finding confidence
### 7.3: Mermaid concept maps
### 7.4: Next research questions + better error messages

## Decisions Made

- New tools return markdown (not JSON) — parsing strategy differs from existing tools
- Pre-pipeline context is injected before graph.ainvoke, not as a graph node
- Cross-domain bridging lives in concept_explorer (not a new node) — needs concept_id + domain context
- Source enrichment in citation_analyzer (has source_ids) rather than literature_search (has snippets)
